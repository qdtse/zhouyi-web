{
    "rewrites": [
        { "source": "/api/(.*)", "destination": "/api/index" }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
          "dest": "/api/index"
        },
        {
            "src": "/(.*)",
            "dest": "/public/$1"
        }
    ]
}
